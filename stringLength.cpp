#include <cctype>
#include <iostream>
using namespace std;

int main()
{
    string str = "king" int length = 0;

    for (int i = 0; str[i] != '\0'; i++)
    {
        length++;
    }
    cout << "the length of the string is:" << length << endl;

    return 0;
}